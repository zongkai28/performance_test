---
datasets:
  # default for each dataset:
  #   name: ''
  #    theme:
  #      color: '#0000ff'
  #      marker:
  #        shape: dot
  #        size: 25
  #        alpha: 1.0
  #      line:
  #        style: solid
  #        width: 2
  #        alpha: 1.0
  #    experiments:
  #      - com_mean: ApexOSPollingSubscription
  #        transport: INTRA
  #        msg: Array1k
  #        pubs: 1
  #        subs: 1
  #        rate: 100
  #        reliability: RELIABLE
  #        durability: VOLATILE
  #        history: KEEP_LAST
  #        history_depth: 16
  #        wildcard: false  <-- set to true wildcard all experiment options except those set
  apex_os_array16k:
    name: Apex.OS + Apex.Middleware Array16k
    theme:
      color: '#652342'
    experiments:
      - msg: Array16k
        wildcard: true
plots:
  - name: compare_apexos_transport
    title: Comparing different Apex.OS Transports with constant message size
    x_axis_label: Time (seconds)
    y_axis_label: Latency (ms)
    x_range: T_experiment
    y_range: latency_mean
    size:
      width: 1000
      height: 600
    # dataset names must be listed above
    datasets:
      - apex_os_array16k